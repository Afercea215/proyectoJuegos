{% extends 'base.html.twig' %}

{% block content %}
    <h1>Editar perfil</h1>
    {{ form_start(form, { 'attr' : { 'class': 'form col-lg-4 col-md-6 col-sm-9 col-xs-12 text-center mt-3' }, 'id':'formRegistro' }) }}
        {{ form_row(form.nombre) }}
        {{ form_row(form.apellidos) }}
        {{ form_row(form.telegramUser) }}
        {# {{ form_row(registrationForm.agreeTerms) }} #}
        <button type="submit" class="w-50 boton mt-3">Guardar</button>
    {{ form_end(form) }}

{% endblock %}